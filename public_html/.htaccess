Options -Indexes

<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteRule (^|/)\.(?!well-known/) - [F,L]
</IfModule>

<FilesMatch "(^\.env|\.ini$|\.log$|\.sql$)">
  Require all denied
</FilesMatch>

